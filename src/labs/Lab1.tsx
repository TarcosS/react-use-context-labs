import { LabShell } from "./LabShell";

export function Lab1() {
  return (
    <LabShell
      title="Lab 1 — Context read patterns"
      description="Compare useContext(MyContext) vs use(MyContext) and discuss composition trade-offs."
    >
      <p style={{ margin: 0, opacity: 0.85 }}>
        Placeholder. Next commit will implement providers and two reading strategies.
      </p>
    </LabShell>
  );
}
