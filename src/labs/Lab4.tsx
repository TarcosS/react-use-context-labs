import { LabShell } from "./LabShell";

export function Lab4() {
  return (
    <LabShell
      title="Lab 4 — Rejection + ErrorBoundary"
      description="Handle rejected promises with ErrorBoundary; define UI transitions clearly."
    >
      <p style={{ margin: 0, opacity: 0.85 }}>
        Placeholder. Next commit will add ErrorBoundary and rejected promise demo.
      </p>
    </LabShell>
  );
}
