import { LabShell } from "./LabShell";

export function Lab2() {
  return (
    <LabShell
      title="Lab 2 — Promise + Suspense"
      description="Compare legacy useEffect loading patterns vs use(promise) + Suspense."
    >
      <p style={{ margin: 0, opacity: 0.85 }}>
        Placeholder. Next commit will add side-by-side implementations.
      </p>
    </LabShell>
  );
}
